

The user reports that after the recent SectionPage UI restructuring (merging Learn/Activities/Quiz into one block card), the Activities and Quizzes that were previously visible no longer show. Let me investigate.

The user is on `/learn` — let me check what page that route renders.
